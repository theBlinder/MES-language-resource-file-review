__pycache__/
*.pyc
.venv/
venv/
uploads/*
!uploads/.gitkeep
output/*
!output/.gitkeep
.DS_Store

# Internal context/handoff doc - kept local only (contains internal notes
# not meant for the public repo). Formerly CLAUDE.md; renamed so it isn't
# auto-loaded into every AI conversation - see its own header note.
PROJECT_ARCHIVE.md
